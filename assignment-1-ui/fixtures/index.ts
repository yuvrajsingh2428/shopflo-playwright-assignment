/**
 * Fixtures barrel export.
 * Spec files import `test` and `expect` from here, not from @playwright/test directly.
 *
 * Usage in spec files:
 *   import { test, expect } from '../../fixtures';
 */
export { test, expect } from './auth.fixture';
